@Library('devops') _

pipeline {
  agent any

  stages {
    stage('Build') {
      steps {
        buildPipeline()
      }
    }

    stage('Code Analysis') {
      steps {
        codeAnalysis()
      }
    }
  }
}