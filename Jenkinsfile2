@Library('devops') _

def buildPipeline() {
  // Lógica para construir el pipeline
}

def codeAnalysis() {
  // Lógica para el análisis de código
}

pipeline {
  agent any

  stages {
    stage('Build') {
      steps {
        script {
          buildPipeline()
        }
      }
    }

    stage('Code Analysis') {
      steps {
        script {
          codeAnalysis()
        }
      }
    }
  }
}