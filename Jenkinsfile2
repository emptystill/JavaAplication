@Library(['emptystill/devops', 'another-library@main']) _

pipeline {
  agent any

  stages {
    stage('Build') {
      steps {
        script {
          emptystill.devops.buildPipelineFunction()
        }
      }
    }

    stage('Code Analysis') {
      steps {
        codeAnalysisFunction()
      }
    }
  }
}
